import { View, Text } from "react-native";
import React from "react";
import appstyles from "../Styles/HomeStyles";

const history = () => {
  return (
    <View style={appstyles.container}>
      <Text style={appstyles.text}>This history</Text>
    </View>
  );
};

export default history;
