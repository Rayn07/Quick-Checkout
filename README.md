Some packages have already been installed (check package.json).
`npm ci` to install node_modules.
`npm run dev` to start the server.

create the environment file yourself with the google mails appcode and respective app_code you get with it.
see the file structure to see how the things should be placed to make sure that all the code runs smoothly without errors
crete the tscnofig and packages.json and others incase not woeking 
